const theme = Object.freeze({
    SEDEBAR: {
        COLOR: {
            DARK: "dark",
            LIGHT: "light",
        },
        VISIBLE: {
            ACTIVE: true,
            INACTIVE: false,
        },
        UNFOLDABLE: {
            ACTIVE: true,
            INACTIVE: false,
        },
    },
    COLOR_SCHEME: {
        AUTO: "auto",
        DARK: "dark",
        LIGHT: "light",
    },
});

const AppConstent = {
    THEMES: theme,
};

export default AppConstent;
